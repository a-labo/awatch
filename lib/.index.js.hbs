/**
 * {{pkg.descrption}}
 * @module {{pkg.name}}
 * @version {{pkg.version}}
 */

'use strict'

const awatch = require('./awatch')

let lib = awatch.bind(this)
Object.assign(lib, {
  awatch
})

module.exports = lib
